name: Initialization

inputs:
    useCache:
        description: Use cache?
        type: boolean
        default: true

runs:
    using: "composite"
    
    steps:
        - name: "Test npm"
          shell: bash
          run: |
            node --version
            npm --version